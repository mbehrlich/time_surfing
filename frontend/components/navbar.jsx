import React from 'react';

const Navbar = ({currentUser}) => {

  return (
    <section class="nav-container">
      <nav class="navbar">
        <div class="logo">Timesurfing</div>
        <ul class="nav-buttons">
          <li>Option 1</li>
        </ul>
      </nav>
    </section>
  );
};

export default Navbar;
